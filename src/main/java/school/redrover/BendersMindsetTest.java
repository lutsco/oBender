package school.redrover;
/*
import school.redrover.Page.*;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class BendersMindsetTest {

    private static final Logger logger = LoggerFactory.getLogger(BendersMindsetTest.class);

    public static void main(String[] args) {
        logger.info("Starting Bender's Mindset Simulation");

        demonstratePage(
                new EntrepreneurPage(),
                "Entrepreneur Page",
                () -> {
                    EntrepreneurPage page = new EntrepreneurPage();
                    page.displayEntrepreneurialTraits();
                    page.demonstrateTrait("creativity");
                    page.generateBusinessIdea();
                    page.shareEntrepreneurTip();
                }
        );

        demonstratePage(
                new SkillsPage(),
                "Skills Page",
                () -> {
                    SkillsPage page = new SkillsPage();
                    page.displaySkills();
                    page.demonstrateSkill("strategy");
                }
        );

        // Repeat for other pages...
    }

    private static void demonstratePage(BasePage page, String pageName, Runnable demonstration) {
        System.out.println("========================================================");
        logger.info("Navigating to {}", pageName);
        page.navigateTo(pageName);
        demonstration.run();
        System.out.println("========================================================");
    }
}

 */
